require 'rubygems'
require 'sinatra'

get '/' do
	erb :it
end

post '/' do
	#MAKE python quiet and JUST print the filename
	name = `python VaporMain.py #{params[:input]}`
	# Ruby script, given the filename, generates an HTML in views/

	#Finally, execute erb :your_html_code
end
